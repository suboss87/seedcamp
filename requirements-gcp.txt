# Optional GCP dependencies — only needed if using Firestore or GCS image uploads.
# Install with: pip install -r requirements-gcp.txt
-r requirements.txt

google-cloud-storage==3.10.0
google-cloud-firestore>=2.16.0
