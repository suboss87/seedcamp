apiVersion: v1
kind: Service
metadata:
  name: seedcamp-dashboard
  namespace: seedcamp
  labels:
    app: seedcamp
    component: dashboard
spec:
  type: LoadBalancer
  selector:
    app: seedcamp
    component: dashboard
  ports:
  - name: http
    port: 80
    targetPort: 8501
    protocol: TCP
