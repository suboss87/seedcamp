apiVersion: apps/v1
kind: Deployment
metadata:
  name: seedcamp-dashboard
  namespace: seedcamp
  labels:
    app: seedcamp
    component: dashboard
spec:
  replicas: 1
  selector:
    matchLabels:
      app: seedcamp
      component: dashboard
  template:
    metadata:
      labels:
        app: seedcamp
        component: dashboard
    spec:
      containers:
      - name: dashboard
        # Replace <instance> with your BytePlus CR instance name
        image: <instance>-ap-southeast-1.cr.bytepluses.com/seedcamp/api:latest
        imagePullPolicy: Always
        command: ["streamlit", "run", "dashboard/app.py", "--server.port", "8501", "--server.address", "0.0.0.0"]
        ports:
        - containerPort: 8501
          name: http
        env:
        - name: API_URL
          value: "http://seedcamp-api.seedcamp.svc.cluster.local"
        resources:
          requests:
            cpu: "250m"
            memory: "256Mi"
          limits:
            cpu: "500m"
            memory: "512Mi"
