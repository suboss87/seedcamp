apiVersion: v1
kind: Service
metadata:
  name: adcamp-dashboard
  namespace: default
  labels:
    app: adcamp
    component: dashboard
spec:
  type: LoadBalancer
  selector:
    app: adcamp
    component: dashboard
  ports:
  - name: http
    port: 80
    targetPort: 8501
    protocol: TCP
